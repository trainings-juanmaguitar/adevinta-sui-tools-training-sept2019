/* eslint-disable */

import domain from '../../raw-live-coding/src/domain/index'
console.log({domain})

  const name = 'juanma'
export default async (params) => {
    console.log({params})
    return { domain, name }
}
